<%= form_for([:admin, @category_badge]) do |f| %>
  <% if @category_badge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category_badge.errors.count, "error") %> prohibited this category_badge from being saved:</h2>

      <ul>
      <% @category_badge.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
